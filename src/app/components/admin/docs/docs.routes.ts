
import { Routes } from '@angular/router';
import { DocsComponent } from './docs.component';

export const docsRoutes: Routes = [
  { path: '', component: DocsComponent, title: 'Documentação' },
];

export default docsRoutes;
